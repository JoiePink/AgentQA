# AgentQA 产品需求文档（PRD）

## 一、文档信息

```text
产品名称：AgentQA
版本：MVP
产品形态：本地 Web 应用
主要用户：Web 前端开发者
首个测试对象：红包管理—新增红包配置
运行环境：本地前端、本地后端接口、本地测试数据
```

## 二、产品目标

AgentQA 帮助前端开发者在提交测试前完成更系统的功能自测。

用户用自然语言描述测试目标，AgentQA 生成包含正常、异常和边界场景的测试计划。用户确认后，系统实际操作页面，检查页面状态、接口请求、接口响应和控制台错误，最后生成带证据的测试报告。

MVP 第一目标是提高测试场景覆盖度，而不是替代专业测试人员。

## 三、产品形态

```text
Vue 管理界面
→ Node.js 后端
→ OpenAI Agent
→ Playwright 测试浏览器
→ 本地数据与报告
```

- AgentQA 运行在用户本机。
- 用户通过浏览器访问 AgentQA 管理界面。
- Node.js 后端负责 Agent、Playwright、任务状态和数据保存。
- MVP 不提供 AgentQA 自身的登录系统。
- OpenAI API Key 和模型名称通过后端 `.env` 配置。
- API Key 不在前端填写或展示。

## 四、页面结构

左侧导航包含：

```text
新建测试
历史任务
Evals
测试配置
```

主要页面：

1. 测试配置页
2. 新建测试任务页
3. 测试计划确认页
4. 测试执行页
5. 测试报告页
6. 历史任务页
7. Evals 页面

## 五、全局业务流程

```text
配置本地测试地址
→ 打开测试浏览器
→ 用户手动登录并进入红包管理页面
→ AgentQA 验证页面可访问
→ 用户输入测试目标
→ AgentQA 生成测试计划和测试数据
→ 用户新增、修改或删除步骤
→ 用户确认测试计划
→ AgentQA 自动执行测试
→ 实时展示推理摘要、操作和结果
→ 收集页面与接口证据
→ 生成在线报告和 HTML 报告
→ 保存历史任务与 Evals 结果
```

## 六、功能需求

### 1. 测试配置

#### 1.1 配置范围

MVP 只保存一份当前测试配置，不提供项目列表。

配置字段：

| 字段 | 必填 | 说明 |
|---|---|---|
| 配置名称 | 是 | 例如“律师华升本地环境” |
| 本地前端地址 | 是 | 前端服务的基础地址 |
| 红包管理页面地址 | 是 | 手动登录后需要进入的目标页面 |
| 本地后端接口地址 | 是 | 允许测试的后端接口基础地址 |

操作按钮：

- 保存配置
- 打开测试浏览器

#### 1.2 地址安全

- 默认只允许 `localhost`、`127.0.0.1` 和明确配置的局域网测试地址。
- 公网地址默认拒绝保存和访问。
- 前端页面和后端请求均不得超出允许地址范围。
- 地址不符合规则时，页面必须说明被拒绝的原因。

### 2. 测试浏览器与手动登录

#### 2.1 打开浏览器

- 点击“打开测试浏览器”后启动独立的 Playwright 可视浏览器。
- 打开已配置的本地前端地址。
- 页面展示浏览器连接状态。

#### 2.2 手动登录

流程：

```text
打开测试浏览器
→ 用户手动登录后台
→ 用户进入红包管理页面
→ 点击“我已登录，开始测试”
→ AgentQA 检查当前页面
```

- AgentQA 不保存用户的登录账号和密码。
- 若仍停留在登录页，禁止创建测试任务。
- 若目标页面无法访问，禁止创建测试任务。
- 登录状态失效时终止正在运行的测试并提示用户。

### 3. 新建测试任务

#### 3.1 输入项

页面只要求用户填写：

| 字段 | 必填 | 说明 |
|---|---|---|
| 测试目标 | 是 | 用户用自然语言描述测试功能和关注点 |

默认示例：

> 测试新增红包功能，检查表单校验、正常提交、接口响应和新增后的列表数据。

系统自动记录：

- 任务名称
- 当前页面地址
- 创建时间
- 当前测试配置

#### 3.2 前置校验

满足以下条件后才能生成计划：

- 测试配置完整有效
- 测试浏览器已连接
- 用户已确认登录
- 目标页面可访问
- 测试目标不为空

### 4. 测试计划

#### 4.1 计划生成

AgentQA 根据测试目标和当前页面生成结构化测试步骤。

计划至少覆盖：

- 正常场景
- 异常场景
- 边界场景

每个测试步骤包含：

| 字段 | 说明 |
|---|---|
| 步骤名称 | 简洁描述测试内容 |
| 场景类型 | 正常、异常或边界 |
| 前置条件 | 执行该步骤所需状态 |
| 操作内容 | Agent 需要执行的页面操作 |
| 测试数据 | 本步骤使用的输入数据 |
| 预期结果 | 判断通过或失败的依据 |

Agent 自动生成有效值、空值、边界值和特殊值等测试数据。

#### 4.2 计划编辑

页面支持：

- 新增步骤
- 修改步骤
- 删除步骤
- 修改测试数据
- 重新生成计划
- 确认并开始执行

交互规则：

- 使用步骤表格展示计划。
- 点击步骤后，通过右侧抽屉查看和编辑详情。
- 新增步骤使用同一个抽屉。
- 新增步骤默认添加到计划末尾。
- MVP 不支持调整步骤顺序。
- 删除步骤前需要二次确认。
- 重新生成计划前提示将覆盖当前修改。
- 执行开始后锁定本次计划和测试数据。

#### 4.3 操作授权

- 用户确认测试计划后，视为已批准计划中的本地操作。
- 计划内的新增提交不重复请求审批。
- Agent 需要执行计划外操作时，必须暂停并请求用户确认。

### 5. 自动执行

MVP 支持：

- 读取当前页面和 URL
- 查找可交互元素
- 点击按钮
- 输入和清空文本
- 选择下拉选项
- 操作数字输入框
- 提交表单
- 读取表单校验提示
- 等待和读取接口响应
- 读取页面提示和列表数据
- 读取浏览器控制台错误
- 页面截图

#### 5.1 可视化虚拟鼠标

测试浏览器必须显示 AgentQA 的虚拟鼠标，让用户能够直接观察 Agent 当前操作的位置和状态。

执行过程：

```text
定位目标元素
→ 高亮目标元素
→ 虚拟鼠标平滑移动到目标
→ 显示当前操作状态
→ 执行点击、悬停或输入
→ 显示操作结果
```

虚拟鼠标状态包括：

```text
定位中
移动中
悬停
点击
输入
等待
失败
```

交互要求：

- 虚拟鼠标必须始终显示在被测浏览器页面内。
- 移动过程需要可见，不能直接瞬移到目标元素。
- 操作前使用蓝色描边高亮目标元素。
- 点击时显示短暂的点击波纹。
- 输入时在鼠标附近显示“正在输入”。
- 等待页面或接口时显示“等待中”。
- 定位失败时显示失败状态，并将原因同步到执行时间线。
- 虚拟鼠标图层必须设置为不可接收指针事件，不得遮挡或改变被测页面交互。
- 页面滚动后需要重新计算目标位置。
- 浏览器缩放或窗口尺寸变化后，虚拟鼠标位置必须保持准确。

Playwright 的实际点击和输入结果是判断依据；虚拟鼠标只负责可视化，不能替代真实自动化操作。

首个场景至少验证：

- 空表单提交时的必填提示
- 单字段缺失时的校验提示
- 红包总金额的最小值和小数精度
- 红包个数的正整数限制
- 有效数据正常提交
- 新增接口请求参数和响应
- 操作成功提示
- 提交后弹窗关闭
- 新增数据出现在列表中

确定性程序判断优先于模型主观判断。

### 6. 测试执行页

#### 6.1 页面布局

```text
顶部：任务摘要、执行进度、停止测试按钮
中部：步骤时间线 + 当前步骤截图
底部：请求、响应、控制台日志标签页
```

- 失败步骤自动展开相关证据。
- 成功步骤默认收起。
- Playwright 浏览器单独显示，不嵌入 AgentQA 页面。
- 用户可以在 Playwright 浏览器中看到虚拟鼠标、目标高亮和操作状态。

#### 6.2 实时状态

步骤状态包括：

```text
等待
执行中
通过
失败
阻塞
未执行
```

状态不能只依赖颜色，必须同时显示图标和文字。

#### 6.3 Agent 过程展示

执行时间线参考 Codex 的展示方式，区分以下内容：

```text
思考：模型提供的可读推理摘要
观察：从页面、接口或控制台获取的信息
计划：准备执行的测试动作
操作：实际调用的浏览器工具
结果：工具和断言执行结果
```

示例：

```text
思考：正在分析新增红包表单的必填规则
观察：发现 8 个表单字段
计划：先验证空表单提交
操作：点击“确定”
结果：出现 8 项必填提示
```

展示规则：

- 推理摘要支持实时流式显示。
- 推理摘要默认折叠，用户可以展开。
- 推理摘要与工具调用、页面观察和验证结果分开显示。
- 不展示隐藏 Prompt 或模型内部原始推理。
- 测试报告只保存必要的推理摘要，不保存冗长过程。
- 如果当前模型不支持推理摘要，页面不显示该区域，不影响测试执行。

OpenAI API 只提供可选的 reasoning summary，不提供原始 reasoning tokens。实现时应按当前模型支持情况请求并展示摘要：

- [OpenAI Reasoning summaries](https://developers.openai.com/api/docs/guides/reasoning#reasoning-summaries)
- [Codex App Server](https://developers.openai.com/codex/app-server)

#### 6.4 停止测试

用户点击“停止测试”后：

- 不再开始新的测试步骤。
- 当前步骤尽快安全结束。
- 剩余步骤标记为“未执行”。
- 保留已产生的结果和证据。
- 生成一份状态为“未完成”的报告。

### 7. 失败处理

- 单个步骤失败时记录结果，并继续执行仍可执行的后续步骤。
- 页面无法继续操作、登录失效或环境中断时终止任务。
- 因前置失败无法执行的步骤标记为“阻塞”。
- 报告中区分失败、阻塞和未执行。
- 不得因为一个断言失败就默认整项任务失败并丢弃后续结果。

### 8. 测试证据

每个步骤保存：

- 步骤状态
- 预期结果
- 实际结果
- 关键截图
- 开始和结束时间

接口相关步骤额外保存：

- 请求地址和方法
- 请求参数
- 响应状态码
- 接口响应

失败步骤额外保存：

- 失败原因
- 控制台错误
- 疑似问题层级

所有证据必须与对应步骤关联。

### 9. 测试报告

MVP 提供在线报告页和 HTML 文件导出。

报告内容：

- 测试目标
- 测试环境
- 开始和结束时间
- 场景覆盖率
- 通过、失败、阻塞和未执行统计
- 每个步骤的预期结果和实际结果
- 页面截图
- 请求参数、状态码和响应
- 控制台错误
- 必要的推理摘要
- 疑似问题层级

问题层级只能使用：

```text
疑似前端问题
疑似接口问题
暂时无法判断
```

证据不足时不得武断归因。

MVP 不提供 PDF 报告。

### 10. 历史任务

历史列表展示：

- 任务名称
- 测试目标
- 执行时间
- 任务状态
- 通过和失败数量
- 场景覆盖率
- 查看报告
- 使用原计划再次运行

重新运行时：

- 使用原测试计划和测试数据创建一次新的运行。
- 每次运行独立保存结果。
- 执行前仍需用户确认计划。

MVP 不提供删除、搜索、分类和批量操作。

### 11. 轻量 Evals

- 使用项目内固定的“新增红包”标准测试场景清单。
- MVP 不提供标准清单编辑页面。
- 对比 Agent 生成计划与标准场景清单。
- 展示已覆盖场景、遗漏场景和覆盖率。
- 保存每次评测结果。

覆盖率计算口径应固定，避免不同版本使用不同规则导致结果不可比较。

## 七、UI 设计要求

### 1. 设计方向

```text
专业开发者工具风
浅色主界面
深色日志区域
紧凑桌面布局
弱化聊天机器人感
```

### 2. 色彩

- 主色：蓝色，用于主要操作和执行中状态。
- 成功：绿色。
- 警告和阻塞：橙色。
- 失败：红色。
- 页面背景：浅灰色。
- 内容区域：白色。
- 日志、JSON、请求和响应：深色代码面板。
- 不使用大面积紫色渐变和霓虹 AI 风格。

### 3. 布局

- 左侧固定导航，展开宽度约 `220px`，支持折叠。
- 顶部显示当前测试配置、浏览器状态和本地环境状态。
- 主内容区使用紧凑的表格、表单和分栏布局。
- 避免大标题、大留白和过高卡片。

### 4. 屏幕适配

- MVP 只适配桌面端。
- 重点适配 `1440px` 宽度。
- 最低支持 `1280px` 宽度。
- 暂不适配手机端。
- 暂不提供整站深色模式和主题切换。

## 八、加载、空状态与错误反馈

- 所有异步操作必须展示加载状态，防止重复提交。
- 生成测试计划时显示明确进度，不允许页面无反馈。
- 无历史任务和无评测结果时展示说明性空状态。
- 配置错误、浏览器断开、登录失效和接口异常必须给出可操作的错误提示。
- 错误提示需要说明发生了什么，以及用户下一步可以怎么处理。

## 九、MVP 不包含

- AgentQA 用户登录系统
- 自动登录被测后台
- 线上环境和生产数据测试
- 微信小程序测试
- 任意网站适配
- 自动修改业务代码
- 多用户和复杂权限系统
- 批量、定时和大规模并发测试
- 完整测试用例管理平台
- 历史任务删除、搜索和分类
- 测试计划步骤排序
- 整站深色模式
- 手机端适配
- PDF 报告

## 十、PRD 验收摘要

MVP 应完成：

1. 保存一份安全受限的本地测试配置。
2. 打开独立测试浏览器并允许用户手动登录。
3. 验证目标页面可访问后创建测试任务。
4. 根据自然语言生成结构化测试计划和测试数据。
5. 允许新增、修改、删除和重新生成计划。
6. 用户确认后自动执行新增红包测试。
7. 在测试浏览器中显示虚拟鼠标、目标高亮和当前操作状态。
8. 实时展示推理摘要、观察、操作和结果。
9. 按规则处理失败、阻塞、停止和环境中断。
10. 保存页面、接口和控制台证据。
11. 生成在线报告和 HTML 报告。
12. 保存历史任务并支持使用原计划再次运行。
13. 使用固定标准清单计算测试计划覆盖率。

以上需求全部达到后，AgentQA MVP 的产品需求视为完成。
