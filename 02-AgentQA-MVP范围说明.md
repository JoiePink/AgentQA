# AgentQA MVP 范围说明

## 一、MVP 目标

完成一个可演示的本地 Web 表单测试闭环：

```text
自然语言测试目标
→ 生成测试计划
→ 用户确认或修改
→ 自动执行页面操作
→ 验证页面和接口结果
→ 收集测试证据
→ 生成测试报告
```

第一版重点验证 AgentQA 能否提高前端开发者的自测场景覆盖度。

## 二、演示范围

```text
项目：lawyer-huasheng 管理后台
模块：红包管理
功能：新增红包配置
环境：本地前端 + 本地后端接口 + 本地测试数据
```

MVP 不连接线上服务器，不操作生产数据。

## 三、核心使用流程

1. 用户填写本地测试地址。
2. AgentQA 打开测试浏览器。
3. 用户在测试浏览器中手动登录后台。
4. 用户确认登录完成。
5. 用户输入自然语言测试目标。
6. AgentQA 检查当前页面并生成测试计划和测试数据。
7. 用户新增、修改或删除测试步骤，并修改测试数据。
8. 用户确认测试计划。
9. AgentQA 自动执行测试。
10. 页面实时展示执行进度。
11. AgentQA 验证结果并收集证据。
12. AgentQA 生成在线报告和 HTML 报告。
13. 用户可从历史任务中再次运行原测试计划。

## 四、必须实现的功能

### 1. 测试浏览器与手动登录

- 根据本地地址打开独立的测试浏览器。
- 由用户手动完成登录。
- 用户确认后才进入测试流程。
- AgentQA 不保存登录账号和密码。
- 登录失效时终止测试并明确提示。

### 2. 自然语言测试目标

- 用户输入需要测试的功能和关注点。
- MVP 示例：`测试新增红包功能，检查表单校验、正常提交、接口响应和新增后的列表数据。`

### 3. 测试计划生成

- 根据测试目标和当前页面生成结构化测试步骤。
- 计划应包含正常、异常和边界场景。
- 每个步骤至少包含操作、测试数据和预期结果。
- Agent 自动生成有效值、空值、边界值和特殊值等测试数据。

### 4. 测试计划确认

执行前允许用户：

- 新增测试步骤
- 修改测试步骤
- 删除测试步骤
- 修改测试数据
- 确认并开始执行

执行开始后锁定本次计划和测试数据。

### 5. 操作授权

- 用户确认测试计划后，视为已批准计划内的本地操作。
- 计划内的新增提交不再重复请求审批。
- Agent 临时增加计划外操作时，必须暂停并请求用户确认。

### 6. 自动执行

MVP 至少支持：

- 打开和识别当前页面
- 点击按钮
- 输入和清空文本
- 选择下拉选项
- 操作数字输入框
- 提交表单
- 读取校验提示
- 等待接口响应
- 读取页面提示和列表数据
- 获取浏览器控制台错误
- 截图

#### 可视化虚拟鼠标

- 测试浏览器中必须显示虚拟鼠标，不能只展示页面操作结果。
- 虚拟鼠标需要平滑移动到目标元素，再执行点击、悬停或输入操作。
- 操作前高亮目标元素，点击时显示波纹反馈。
- 鼠标附近显示当前状态：定位中、移动中、悬停、点击、输入和等待。
- 目标定位失败时，虚拟鼠标状态显示失败，并与当前测试步骤关联。
- 虚拟鼠标只用于展示，不得遮挡页面或影响元素定位和点击。

### 7. 结果验证

至少验证：

- 必填校验是否出现
- 页面提示是否符合预期
- 请求参数是否符合测试数据
- 接口状态码和响应是否成功
- 提交成功后弹窗是否关闭
- 新增数据是否出现在列表中

确定性程序判断优先于模型主观判断。

### 8. 失败处理

- 单个步骤失败时记录结果，并继续执行仍可执行的后续步骤。
- 页面无法继续操作、登录失效或测试环境中断时终止任务。
- 因前置失败无法执行的步骤标记为“阻塞”。
- 报告中区分“失败”“阻塞”和“未执行”。

### 9. 测试证据

- 每个步骤保存执行状态、实际结果和关键截图。
- 接口相关步骤保存请求参数、状态码和响应。
- 失败步骤额外保存失败原因和控制台错误。
- 证据与对应测试步骤关联。

### 10. 实时执行时间线

- 实时展示当前步骤、操作内容、状态和结果。
- 状态至少包含：等待、执行中、通过、失败、阻塞和完成。
- 不向用户展示模型内部思考过程。

### 11. 测试报告

MVP 提供：

- 在线测试报告页
- HTML 文件导出
- 测试目标和运行环境
- 测试步骤及其结果
- 通过、失败、阻塞和未执行数量
- 页面截图和接口证据
- 失败原因
- 执行时间

MVP 不提供 PDF 报告。

### 12. 历史任务与重新运行

- 保存历史测试任务、测试计划和运行结果。
- 支持使用原测试计划再次运行。
- 每次运行独立保存结果。
- MVP 不提供分类、批量执行和版本管理。

### 13. 轻量 Evals

- 为“新增红包”建立标准测试场景清单。
- 将 Agent 生成的计划与标准清单进行对比。
- 统计计划覆盖率和遗漏场景。
- 保存每次评测结果，便于比较不同版本。

## 五、MVP 明确不做

- 自动登录
- 线上环境和生产数据测试
- 微信小程序测试
- 任意网站适配
- 自动修改业务代码
- 完全替代专业测试人员
- 多用户和复杂权限系统
- 批量、定时和大规模并发测试
- 完整测试用例管理平台
- PDF 报告

## 六、MVP 验收条件

使用本地“红包管理—新增红包配置”页面完成一次端到端演示，并满足：

1. 用户手动登录后可以发起测试。
2. AgentQA 能生成包含正常、异常和边界场景的计划。
3. 用户能新增、修改和删除步骤及测试数据。
4. 确认计划后能自动操作页面并提交本地接口。
5. 测试浏览器能显示虚拟鼠标、目标高亮和当前操作状态。
6. 能验证表单提示、请求、响应和新增后的列表结果。
7. 单步失败时能按规则继续或终止。
8. 能实时展示执行进度。
9. 能输出包含步骤结果、截图和接口证据的在线及 HTML 报告。
10. 能保存历史任务并重新运行原计划。
11. 能通过标准场景清单计算计划覆盖率。

以上条件全部满足后，AgentQA MVP 视为完成。
