# AgentQA 项目概述

## 一、项目背景

项目发起人是一名前端开发工程师。随着 AI 编程能力不断增强，传统前端开发岗位的工作内容和能力要求正在变化。项目发起人希望结合已有的 Vue、Node.js 和 Web 开发经验，研发一个具备真实业务闭环的 Agent 产品，积累 AI 应用和 Agent 工程实践经验，为职业转型做准备。

前端开发者完成页面功能后，通常需要自行检查数据展示、表单校验和增删改操作。人工自测容易受到个人经验、思维习惯和注意力的影响。开发者往往沿着自己预期的正确路径验证功能，难以主动发现异常操作和边界问题，因此经常出现开发者认为功能正常，但交给测试人员后仍发现较多问题的情况。

AgentQA 希望利用 Agent 的任务规划和浏览器自动化能力，辅助开发者分析测试目标、补充测试场景、实际操作页面并保存测试证据，从而提高开发阶段的自测覆盖度。

AgentQA 不承诺发现所有错误，也不以完全替代专业测试人员为目标。

## 二、目标用户

第一版主要面向需要在提交测试前进行功能自测的 Web 前端开发者。

测试对象是完整的 Web 业务流程，而不只是前端代码。AgentQA 需要同时观察页面状态、浏览器控制台、网络请求和接口响应，帮助开发者发现可能来自页面交互、前端参数、后端接口或前后端协作的问题。

第一版以个人开发者自测为主要场景，不面向大型专业测试团队设计复杂的测试管理功能。

## 三、用户痛点

前端开发者通常依靠个人记忆和开发经验，边操作页面边判断功能是否正常。数据显示功能主要检查接口响应与页面展示是否一致；新增、删除和修改功能通常只执行一次正常操作，再检查接口是否成功。

这种方式存在以下问题：

1. 测试前缺少明确的测试计划。
2. 测试覆盖依赖个人经验，容易遗漏异常和边界场景。
3. 开发者熟悉功能实现，容易按照正确方式操作并产生思维盲区。
4. 功能修改后需要重复人工操作，测试过程难以复用。
5. 页面、请求、响应和控制台错误等证据比较分散。
6. 页面异常时，问题所在层级不容易判断。

## 四、产品定位

AgentQA 是一款面向 Web 前端开发者的智能自测 Agent。

开发者提供测试环境、测试账号和自然语言测试目标后，AgentQA 会生成结构化测试计划。开发者确认或修改计划后，系统通过浏览器自动执行页面操作，同时检查页面状态、网络请求、接口响应和控制台错误，最终输出带有执行步骤、判断结果和相关证据的测试报告。

一句话定位：

> AgentQA：帮助前端开发者规划并执行 Web 功能自测的智能测试 Agent。

核心流程：

```text
理解测试目标
→ 生成测试计划
→ 用户确认或修改
→ 实际操作 Web 页面
→ 验证页面和接口结果
→ 收集问题证据
→ 输出测试报告
```

## 五、核心价值

AgentQA 的第一核心价值是提高前端开发者的自测覆盖度。

AgentQA 从正常流程、异常流程、边界条件和接口异常等角度生成测试计划，补充开发者依靠个人记忆和经验可能遗漏的场景。确认计划后，系统实际操作页面并验证结果，为每个场景保存执行结果和相关证据。

测试计划可以重复执行，使不同版本尽可能按照相同范围完成回归测试。

## 六、核心使用场景

第一版使用以下真实场景进行开发和演示：

```text
项目：lawyer-huasheng 管理后台
模块：红包管理
功能：新增红包配置
运行环境：本地前端项目 + 本地后端接口 + 本地测试数据
```

新增红包表单包含：

- 发送方名称
- 红包类型
- 红包祝福语
- 红包总金额
- 红包个数
- 到账方式
- 最小会员等级
- 状态

开发者向 AgentQA 输入：

> 测试新增红包功能，检查表单校验、正常提交、接口响应和新增后的列表数据。

AgentQA 生成并执行包含以下内容的测试计划：

1. 所有字段为空时提交，检查必填提示。
2. 分别遗漏字段，检查对应校验。
3. 检查红包总金额的最小值和小数精度。
4. 检查红包个数只能填写正整数。
5. 使用有效数据完成提交。
6. 检查新增接口请求参数和响应。
7. 检查页面是否提示操作成功并关闭弹窗。
8. 检查新增红包是否出现在列表中。
9. 保存步骤结果、截图、接口证据和控制台错误。

AgentQA 只连接本地测试环境，不访问线上服务器或操作生产数据。

## 七、项目目标与成功标准

第一版达到以下结果，即视为基本成功：

- 能根据自然语言生成新增红包测试计划。
- 测试计划包含正常、异常和边界场景。
- 用户可以确认或修改测试计划。
- 能自动操作本地页面并调用本地接口。
- 能验证页面、请求、响应和列表结果。
- 能输出简洁的测试报告及错误证据。
- 测试计划的覆盖度高于开发者临时手动自测。

## 八、项目边界

第一版不包含：

- 线上环境测试
- 微信小程序测试
- 支持任意网站
- 自动修改业务代码
- 完全替代测试人员
- 大规模并发测试
- 复杂用户与权限系统

第一版聚焦一个目标：完成“自然语言测试目标—测试计划—浏览器执行—结果验证—证据报告”的本地 Web 表单测试闭环。
